# Required by the MagicUtils matrix-settings plugin (loadPublishingSpec), but
# CommandFlow is NOT published to a Maven repository — its release channel is
# Modrinth. These values are placeholders so the matrix plugin resolves; no
# Maven publish task is run for CommandFlow.
group=dev.ua.theroer
repo.url=https://maven.theroer.dev/releases
smoke.artifact=commandflow-bukkit
